import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'BoTRT — Autenticação',
};

export default function BotrtCallbackLayout({ children }: { children: React.ReactNode }) {
    return children;
}
